{
  "name": "raec-rooms-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Signage board for Romeoville Athletic & Event Center",
  "scripts": {
    "fetch": "node scripts/fetch_email.js",
    "transform": "node scripts/transform.mjs",
    "build": "npm run fetch && npm run transform"
  },
  "dependencies": {
    "imapflow": "^1.0.199",
    "mailparser": "^3.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
